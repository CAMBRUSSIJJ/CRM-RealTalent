# Fonte oficial — V100.48

A fonte oficial permanece em `src/`, usando React e TypeScript. Os arquivos em `dist/` e os HTML standalone são gerados exclusivamente pelos scripts de build.

A V100.48 adiciona:

- `src/components/ui/action-dialog-center.tsx`;
- `src/styles/actions-system.css`;
- APIs `confirmAction`, `promptAction` e toast acionável no App Context.

Não edite manualmente o bundle em `dist/` ou os arquivos standalone.
