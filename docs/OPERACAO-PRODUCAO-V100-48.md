# Operação em produção — V100.48

A versão não altera contratos de banco ou tabelas. O deploy exige publicação do build web e confirmação do `public/health.json` na versão `100.48`.

Antes da promoção, execute os testes, o build oficial e as auditorias de identificadores, renderização, ações profissionais e prontidão de produção.
