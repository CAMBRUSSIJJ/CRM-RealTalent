-- O primeiro usuário e o workspace são criados pela interface após o cadastro.
-- As etapas padrão e as configurações iniciais são criadas pela função
-- public.create_organization_with_defaults, aplicada pelas migrations.
select 1;
